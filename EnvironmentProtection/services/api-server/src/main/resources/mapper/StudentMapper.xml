<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lvfat.repository.StudentMapper">
    
    <!-- 分页查询学生 -->
    <select id="selectStudentPage" resultType="com.lvfat.entity.Student">
        SELECT * FROM student
        WHERE deleted = 0
        <if test="schoolId != null and schoolId != ''">
            AND school_id = #{schoolId}
        </if>
        <if test="classId != null and classId != ''">
            AND class_id = #{classId}
        </if>
        <if test="grade != null">
            AND grade = #{grade}
        </if>
        <if test="studentName != null and studentName != ''">
            AND student_name LIKE CONCAT('%', #{studentName}, '%')
        </if>
        <if test="studentStatus != null and studentStatus != ''">
            AND student_status = #{studentStatus}
        </if>
        ORDER BY grade, student_no
    </select>
    
</mapper>
